const config = {
  organization: "drawdb-io",
  reportOutputDir: "reports",
  chartOutputDir: "charts",
  trackedPeriod: 1000,
  chartSettings: {
    width: 960,
    height: 600,
    backgroundColour: "",
  },
};

export default config;
